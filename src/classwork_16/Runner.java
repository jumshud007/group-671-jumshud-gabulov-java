package classwork_16;

import java.util.Scanner;

public class Runner {
	public static void main(String[] args) {
//	### Задание 1 (базовое)
//	Напишите программу, которая:
		Scanner s = new Scanner(System.in);
		int a = s.nextInt();
		Scanner s1 = new Scanner(System.in);
		int a1 = s1.nextInt();
		int b = a - a1;
		double e = a / a1;
		System.out.println(a + " + " + a1 + " = " + a + a1);
		System.out.println(a + " - " + a1 + " = " + b);
		System.out.println(a + " * " + a1 + " = " + a * a1);
		if (a1 != 0) {
			System.out.println(a + " / " + a1 + " = " + e);
		} else {
			System.out.println("На ноль делить нельзя");
		}
//	- запрашивает у пользователя два целых числа
//	- выводит их сумму, разность, произведение и частное (с плавающей точкой)
//	- если второе число = 0 → вместо деления выводит сообщение «На ноль делить нельзя»
//
//	Пример вывода:
//
//	Введите два числа: 18 7
//	18 + 7 = 25
//	18 - 7 = 11
//	18 * 7 = 126
//	18 / 7 = 2.5714285714285716
//
//
//	### Задание 2 (условия + методы)
//	Напишите метод isLuckyTicket(int number), который принимает 6-значное число и возвращает true, если это «счастливый билет» (сумма первых трёх цифр равна сумме последних трёх).
		Scanner s2 = new Scanner(System.in);
		int num = s2.nextInt();
		if (Integer.parseInt((num + "").split("")[0])
				+ Integer.parseInt((num + "").split("")[1])
				+ Integer.parseInt((num + "").split("")[2]) == Integer.parseInt((num + "").split("")[3])
						+ Integer.parseInt((num + "").split("")[4]) + Integer.parseInt((num + "").split("")[2])
						+ Integer.parseInt((num + "").split("")[5])) {
			System.out.println(true);
		} else {
			System.out.println(false);
		}
//	Примеры:
//	- 123006 → true (1+2+3 = 6)
//	- 123456 → false
//	- 111111 → true
//	- 000999 → true
//
//	В main протестируйте минимум на 6 разных числах (в том числе с ведущими нулями).
//
//	### Задание 3 (массивы + логика)
//	Дан массив целых чисел (можно захардкодить или ввести с клавиатуры).
//
//	Напишите программу, которая выводит:
//
//	1. Сумму всех элементов
//	2. Количество положительных чисел
//	3. Количество отрицательных чисел
//	4. Максимальное значение
//	5. Минимальное значение
//	6. Среднее арифметическое (с точностью до 2 знаков после запятой)
//
//	Пример массива: {-4, 12, 0, -9, 5, 3, -1, 10}
//
//	### Задание 4 (строки + циклы)
//	Напишите программу, которая:
//
//	- запрашивает у пользователя строку
//	- выводит эту строку задом наперёд
//	- считает и выводит количество гласных букв (а, е, ё, и, о, у, ы, э, ю, я) — без учёта регистра
//	- заменяет все пробелы на символ подчёркивания _ и тоже выводит результат
//
//	Пример:
//
//	Введите строку: Привет как дела
//	Строка задом наперёд: алед ак тевирП
//	Гласных букв: 6
//	После замены пробелов: Привет_как_дела
//
//
//	### Задание 5 (немного посложнее — класс + массив объектов)
//	Создайте класс Student с полями:
//
//	- String fullName
//	- int course
//	- double averageGrade
//
//	Создайте массив из 5–7 студентов (можно захардкодить).
//
//	Выведите:
//
//	1. Всех студентов курса 2 (если есть)
//	2. Средний балл по всей группе
//	3. Студента с самым высоким средним баллом (если несколько — вывести любого)
//	4. Отсортированный по убыванию среднего балла список всех студентов (можно просто вывести имена и баллы)
	}
}
